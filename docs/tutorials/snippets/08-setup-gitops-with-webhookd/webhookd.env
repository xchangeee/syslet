WHD_LISTEN_ADDR=:38080
WHD_HOOK_SCRIPTS=/etc/webhookd/scripts
WHD_PASSWD_FILE=/etc/webhookd/users.htpasswd
# Pulling images can take a while, the default is 10 seconds
WHD_HOOK_TIMEOUT=600
# Run one deployment at a time
WHD_HOOK_WORKERS=1
WHD_LOG_MODULES=hook
