# Workflow

TODO: the intended overall workflow (spec → validate → diff → apply → summary), the apply phase ordering (stop changed/removed → write files → daemon-reload → start → prune), and the restart/reload/no-op decision matrix.
