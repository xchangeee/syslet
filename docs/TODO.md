# Docs TODO

Open items for the docs.
Pages that wait on an item carry an HTML comment `<!-- TODO: ... see docs/TODO.md -->` at the spot, which doesn't render.

## Docs

- **Alternatives and comparisons is a stub** — [alternatives-and-comparisons.md](explanation/alternatives-and-comparisons.md) only lists rough axes, so it's left out of the nav and not linked. Decide on the positioning, write it as a discussion, then add it to `zensical.toml`, [explanation/index.md](explanation/index.md) and [Why syslet](explanation/why-syslet.md).
- **Install instructions assume a GitHub release** — [01-installation.md](tutorials/01-installation.md) tells users to download a GoReleaser tarball from `github.com/xchangeee/syslet/releases`. The canonical remote is still Codeberg and no release has been published, so the URL and the archive names (`syslet_Linux_x86_64.tar.gz`) are unverified. Confirm the mirror/release setup, then check the link.

