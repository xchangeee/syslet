# Build a container image

TODO: customize an upstream container image without a registry, using the `build` unit type — layer local changes (config baked in, added files, patched packages) onto a `FROM` upstream image locally.
